/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  eslint: {
    ignoreDuringBuilds: true, // disables all ESLint warnings/errors during dev and build
  },
};

export default nextConfig;
